#include <bits/stdc++.h>

#include "lib/prelude.h"

void solve() {
    //
}

i32 main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    u32 t;
    std::cin >> t;

    while (t--) solve();
}
